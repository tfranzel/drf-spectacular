openapi: 3.0.3
info:
  title: ''
  version: 0.0.0
paths:
  /v1/x/:
    get:
      operationId: v1_x_list
      description: ''
      tags:
      - v1
      security:
      - cookieAuth: []
      - basicAuth: []
      - {}
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Xv1'
          description: ''
components:
  securitySchemes:
    cookieAuth:
      type: apiKey
      in: cookie
      name: Session
    basicAuth:
      type: http
      scheme: basic
  schemas:
    Xv1:
      type: object
      properties:
        id:
          type: integer
      required:
      - id
